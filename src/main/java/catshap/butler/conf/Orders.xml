<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orders">

	<select id="getBeforeOrders" parameterType="map" resultType="int">
	    SELECT COUNT(*)
	    FROM ORDERS o
	    JOIN ORDERPRODUCT op ON o.ORDNO = op.ORDNO
	    WHERE o.USERNO = #{userNo}
	    AND op.PRODNO = #{prodNo}
	    AND o.ORDSTATUS = '주문중'
	</select>

	<select id="selectProductPrice" parameterType="int" resultType="ProductPrice">
		select *
		from prodPrice
		where prodNo=#{prodNo}
	</select>

	<insert id="insertOrders" parameterType="Orders" useGeneratedKeys="true" keyProperty="ordNo">
		<selectKey keyProperty="ordNo" resultType="int" order="AFTER">
            SELECT seq_ordNo.currval FROM dual
        </selectKey>
		insert into orders
		values (seq_ordNo.nextval, #{userNo}, sysdate, #{ordTotalPrice}, '주문중')
	</insert>
	
	<insert id="insertOrderProduct" parameterType="OrderProduct">
		insert into orderProduct
		values (seq_ordProdNo.nextval, #{prodNo}, #{ordNo}, #{ordProdAmt}, #{ordProdPrice})
	</insert>
	
	<select id="getOrderProductPrice" parameterType="int" resultType="int">
		SELECT
		    SUM(op.ordProdAmt * op.ordProdPrice) AS totalOrderPrice
		FROM
		    ORDERS o
		JOIN
		    ORDERPRODUCT op ON o.ORDNO = op.ORDNO
		JOIN
		    PRODUCT p ON op.PRODNO = p.PRODNO
		WHERE
		    o.USERNO = #{userNo}
		    AND o.ORDSTATUS = '주문중'
	</select>
	
	<select id="getOrderProductList" parameterType="int" resultMap="OrderProductMap">
		SELECT
		    op.PRODNO, p.PRDOTITLENAME, op.ORDPRODAMT, op.ORDPRODPRICE ,o.ORDTOTALPRICE, pi.PRODIMGPATH 
		FROM
		    ORDERS o
		JOIN
		    ORDERPRODUCT op ON o.ORDNO = op.ORDNO
		JOIN
		    PRODUCT p ON op.PRODNO = p.PRODNO
		JOIN 
			PRODIMAGE pi ON pi.PRODNO = p.PRODNO 
		WHERE
		    o.USERNO = 1
		    AND o.ORDSTATUS = '주문중'
	</select>
	
	<resultMap id="OrderProductMap" type="OrderProductList">
        <result column="PRODNO" property="prodNo" />
        <result column="PRDOTITLENAME" property="prodTitleName" />
        <result column="ORDPRODAMT" property="ordProdAmt" />
        <result column="ORDPRODPRICE" property="ordProdPrice" />
        <result column="ORDTOTALPRICE" property="ordTotalPrice" />
        <result column="PRODIMGPATH" property="prodImgPath" />
    </resultMap>
	
</mapper>
